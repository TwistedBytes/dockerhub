<IfDefine ! _USER>
Define _USER www-data
</IfDefine>
Define _GROUP www-data

Define _HTTP_PORT 80
Define _HTTPS_PORT 443

Define _LOGDIR_WEB /var/log/httpd

Define _DOCROOT /var/www/html/public

Define _PHPFPM_PORT php-fpm:9000
